import { ToolbarC } from './components/index.js'

m.render(document.querySelector('.topbar'), [
	m(ToolbarC),
])

// m.render(document.querySelector('.maincontent'), [
// 	// m(ToolbarC),
// ])

// m.route(root, "/splash", {
// 	"/splash": Splash,
// 	"/hello": Hello,
// })